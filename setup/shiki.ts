import { defineShikiSetup } from "@slidev/types";

export default defineShikiSetup(() => {
    return {
        themes: {
            // dark: "dark-plus",
            // light: "light-plus",
            // dark: "github-dark",
            // light: "github-light",
            dark: "one-dark-pro",
            light: "github-light",
            // light: "one-light",
        },
    };
});
