## Slidev / Markdown rules

- HTML blocks in markdown must never contain empty lines. Empty lines cause the
  markdown parser to break the HTML into separate blocks, corrupting the
  rendered output.
